{
  "name": "pixel-tabletop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Digital pixel-art tabletop with exact unit positioning for Warhammer 40k / Age of Sigmar and D&D 5E.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "predev": "node scripts/ensure-deps.mjs",
    "dev": "vite",
    "prebuild": "node scripts/ensure-deps.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "pretest": "node scripts/ensure-deps.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
