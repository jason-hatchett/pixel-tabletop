## What & why

<!-- One or two sentences. Reference `path:line` instead of pasting diffs. -->

## Checklist
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Every domain change (`src/domain/`) has a vitest test
- [ ] Nothing stores pixels/grid-squares in the domain — everything is millimetres
- [ ] UI/rendering changes verified live in the Browser pane (not just tests)
